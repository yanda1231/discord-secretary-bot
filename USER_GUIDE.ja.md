# Discord秘書bot ユーザーガイド

このbotは、Discord上でtodo・リマインダー・雑談・朝の作戦会議・夜の締めを手伝う秘書botです。

使い方はシンプルで、覚えることは実質3つだけです。

1. **各チャンネルに普通の日本語で書く**（コマンドを覚えなくていい）
2. **botが確認を出したら「確定」ボタンを押す**
3. **返事は最大1分待つ**（botは1分ごとに巡回しています）

---

## まず知っておくこと

### チャンネルの役割

| チャンネル | 使い方 |
|---|---|
| `#todo` | やることを書く場所。例: `明日までに資料を作る`。期限が足りない場合は、確認画面から自然文で追加できます |
| `#reminder` | 思い出させてほしいことを書く場所。例: `10分後に水を飲む`。日時が足りない場合は、確認画面から自然文で追加できます |
| `#雑談` | ユウカと会話する場所。普通の雑談のほか、todo候補・リマインダー候補・支出メモ候補もここから拾ってくれます |
| `#報告` | ユウカからの通知を受け取る場所。朝の作戦会議、夜の締め、期限通知、リマインダー通知、月末支出レポートが届きます。基本的に一方通行で、ここで話しかけても雑談処理はしません |
| `#設定` | botの設定を変更する場所。キャラ設定ファイル、朝会・夜締めの時刻、事前通知、チャンネル設定に使います |
| `#つぶやき` | botが反応しない独り言・メモ用の場所。思いつき、感情メモ、あとで見返したい断片をどうぞ。AI応答もtodo登録もされません |

### botの反応の仕組み

- **返事は最大1分遅れます。** botは常時接続ではなく、1分ごとに新しい投稿を確認しています。反応がなくても少し待ってみてください
- **とても長い文章は一部省略されることがあります。** Discordの1メッセージには文字数制限があるため、長いキャラ設定・長い雑談・支出レポート・todo一覧などは上限に合わせて省略されることがあります。日常利用ではほぼ気になりませんが、非常に長い文章は分けて投稿してください

---

# 第1章 毎日の使い方：todoとリマインダー

## 1-1. todoを登録する

`#todo` に普通に書きます。

```text
明日までに資料を作る
```

botが内容と期限を読み取り、確認を出します。

```text
todo候補を検出しました。
内容: 資料を作る
期限: 2026-07-02T18:00:00+09:00
事前通知: 15分前

[確定] [取り消し]
```

内容が合っていれば `確定` を押します。違っていれば `取り消し` を押して、もう一度書き直します。

**期限を書かなかった場合**は、期限なしtodoとして確認されます。

```text
先生、期限の変数が未定です。期限なしでtodoにしますか？
[確定] [取り消し]
[期限を設定]
```

期限を付けたいときは `期限を設定` を押すと入力欄が開くので、自然な言い方で書けます。

```text
明日18時
今日22時
7月3日18時
```

入力すると、候補メッセージの `期限: 未設定` が新しい期限に更新されます。合っていれば `確定` を押します。

## 1-2. リマインダーを登録する

`#reminder` に普通に書きます。

```text
10分後に水を飲む
```

botが内容と日時を読み取り、確認を出します。

```text
リマインダー候補を検出しました。
内容: 水を飲む
日時: 2026-07-01T20:30:00+09:00
事前通知: 15分前

[確定] [取り消し]
```

**日時が読み取れなかった場合**は、`日時を設定` ボタンが出ます。

```text
先生、日時の変数が未定です。いつ通知するか設定してください。
[確定] [取り消し]
[日時を設定]
```

押すと入力欄が開くので、自然な言い方で書けます。

```text
10分後
今日22時
明日朝
```

入力すると、候補メッセージの `日時: 未設定` が新しい日時に更新されます。

### 繰り返しリマインダー

「**毎**」を付けて書くだけで、繰り返しリマインダーになります。

```text
毎日9時に薬
毎週月曜9時にゴミ出し
毎月1日12時に家賃確認
```

確認画面に `繰り返し: 毎週月曜` のように表示されます。仕組みはこうです。

- 通知が済むと、自動で次回分が予約されます
- 「毎月31日」のように短い月に存在しない日は、その月の月末に自動で寄せます
- 繰り返し通知をスヌーズしても、ずれるのは今回の分だけ。来週以降の予定は崩れません

## 1-3. 通知の受け取り方

事前通知が有効な場合、通知は2回届きます。

- **事前通知** — 15分前、30分前など（設定した分数前）
- **実時間通知** — 指定した時刻ちょうど

```text
リマインダーの事前通知です。(15分前)
#2 水を飲む
予定: 2026/07/01 20:30
```

```text
リマインダーです。
#2 水を飲む
予定: 2026/07/01 20:30

[対応済み]
[5分後] [30分後] [1時間後] [3時間後] [明日]
```

- `対応済み` を押すと、そのリマインダーは完了扱いになります
- まだ対応できないときはスヌーズ（`5分後` など）を押します。`明日` は翌朝9:00に再通知します

**todoの期限時刻**には、こちらのボタンが出ます。

```text
[完了] [まだ終わってない]
```

`まだ終わってない` を押すと、再通知のタイミングを選べます（`5分後` `30分後` `1時間後`）。この再通知はtodoの期限そのものを変えるものではなく、通知だけをもう一度出します。

---

# 第2章 雑談チャンネルでできること

## 2-1. 普通に雑談する

`#雑談` では、普通に会話できます。

```text
今日ちょっと疲れた
```

botはキャラクター設定に沿って短く返します。

**会話を続けたいときはリプライを使います。** ユウカの返答にDiscordのリプライで返してください。`#雑談` ではユウカも先生の発言へリプライで返すため、リプライ同士がつながっている間だけ、その会話の始まりから直近まで（過去最大8ラリー）を文脈として読みます。リプライではない新規投稿は、過去の雑談を文脈として読みません。

## 2-2. 雑談から予定やtodoを拾ってくれる

雑談中に予定やtodoらしい発言があると、候補として拾います。

```text
明日の19時に駅行かなきゃ
```

```text
リマインダー候補を検出しました。
内容: 駅に行く
日時: 2026-07-02T19:00:00+09:00

[確定] [取り消し]
```

**1つの投稿に複数の話が混ざっていても、分けて処理します。**

```text
今日ちょっと疲れた。あと明日19時に駅行かなきゃ。資料も終わった。
```

この場合、必要に応じて以下が起きます。

- 疲れたことへの短い返答
- 明日19時のリマインダー候補
- 資料todoの完了確認
- 支出・浪費メモ候補

## 2-3. 完了報告する

`#雑談` で完了報告らしい発言をすると、botが未完了todoと照らし合わせます。

```text
資料作るの終わった
```

```text
先生、「資料を作る」は完了でよろしいですか？
問題なければ、完了として記録しておきます。

[完了にする] [違う]
```

スラッシュコマンド `/todo done` でも完了できます。IDを入れなくても、未完了todoの候補ボタンが出ます。

## 2-4. 支出・浪費メモ

`#雑談` で支出や浪費っぽい話をすると、**金額が明確な場合だけ**botが候補として拾います。支出は **大分類・品物・店・金額** で管理します。

```text
食費で、牛乳とパンをイオンで680円使った
```

```text
支出候補を検出しました。
大分類: 食費
品物: 牛乳とパン
店: イオン
金額: 680円
日付: 9/5

[記録する] [記録しない]
```

`記録する` を押すと、支出メモとして保存します。

大分類は「食費、雑費、日用品、交通費、交際費、医療費、趣味、サブスク、住居、その他」の固定一覧です。候補カードのプルダウンから変更できます。カードにリプライして `雑費にして、店はまいばすけっと` のように言い直すこともできます。候補が1件だけなら、カードなしで `品物は電池` や `500円だった` と書いても訂正できます。期限切れや処理済みのカードへの訂正は新しい候補として重複登録されません。

**月末日の12:00**に、その月の支出レポートが `#報告` に届きます。2月の28日、うるう年の29日、30日、31日など、月の日数が違っても月末を自動で判定します。

## 2-5. 機能要望を受け付ける

機能の要望は #雑談 で「要望：〜」と言えば受け付けます。`/request list` で受付状況を見られます。

---

# 第3章 一覧とスラッシュコマンド

## 3-1. todo一覧

```text
/todo list
/todo list range: today
/todo list range: week
/todo list range: all
```

`range` を省略すると `all` になります。一覧にも完了ボタンが出ます。

## 3-2. リマインダー一覧

```text
/reminder list
```

一覧では `[単発]` と `[繰り返し]` が区別して表示されます。削除ボタンも出るので、不要になったものは一覧から選んで削除できます。繰り返しリマインダーを削除すると、今後の分すべてが削除されます。

`#雑談` で自然文でも削除できます。

```text
昼ごはんのリマインダー消して
```

botが対象を確認してから削除します。

## 3-3. 支出メモ一覧

```text
/expense list
/expense list range: month
/expense list range: all
```

`range` を省略すると `all` になり、直近全体の支出メモを直近10件の大分類→小分類の階層で表示します。各大分類の小計の下に、品物と店の明細が並びます。間違って登録した支出メモは、一覧の削除ボタンで削除できます。`month` を指定すると今月分です。

## 3-4. 機能要望一覧

```text
/request list
```

自分が #雑談 で受け付けた機能要望の直近20件を、受付状況つきで確認できます。

## 3-5. バックアップ

```text
/backup
```

todo・リマインダー・支出メモ・設定・キャラ設定をまとめたバックアップファイル（JSON）が `#報告` に届きます。

何もしなくても、**月初に自動でバックアップファイルが `#報告` に届きます**。万一データが消えた時は、このファイルから復元できるので、消さずに置いておいてください（復元作業自体は作成者やAIに頼めばOKです）。

## 3-6. 使い方を確認する

```text
/guide
```

使えるスラッシュコマンドを、ユウカが定型文で説明します。

---

# 第4章 ユウカからの定期レポート

## 4-1. 朝の作戦会議

設定した時刻になると、`#報告` に今日の予定が届きます。内容はこちらです。

- 今日のtodo
- 今日のリマインダー
- リスク注意
- 長く残っているtodoの因数分解提案
- 同じ日に予定/todoが6件以上ある時のレア台詞
- 最後に、ユウカからの短い締めの一文

```text
先生、今日の作戦会議です。今日の変数を確認しました。

todo (today)
#3 資料を作る - 2026-07-02T18:00:00+09:00

リマインダー
#4 駅に行く - 2026-07-02T19:00:00+09:00

期限24時間以内の未完了が3件あります。ほんの少しだけ、優先順位を意識しましょう。
まずは「資料を作る」を15分だけ進めましょう。因数分解すれば、必ず解けます。
```

予定やtodoが6件以上ある日は、レア台詞が入るかも？

## 4-2. 夜の締め

設定した時刻になると、`#報告` に一日のまとめが届きます。

- 今日完了したtodo数
- 明日のtodo
- ユウカからの労い

## 4-3. リスク注意の基準

botは、期限前に何でも警告するわけではありません。負担が高そうな時だけ朝会で注意します。現状の判定はこちらです。

- 期限24時間以内の未完了が3〜4件 → 軽く注意
- 期限24時間以内の未完了が5件以上 → 明確に注意
- 期限3時間以内の未完了が5件以上 → 心配
- 期限2時間以内の未完了が4件以上 → 心配
- 期限1時間以内の未完了が3件以上 → 強めに心配
- 期限超過1日目 → 朝に軽く確認
- 期限超過2日目以降 → ユウカらしく呆れつつ因数分解提案
- 同じ日に予定/todoが6件以上 → レア台詞で強めに警告

## 4-4. 先延ばしtodoの因数分解

未完了todoが残っていると、botがタスクを小さく分ける提案をします。対象はこちらです。

- 期限超過2日目以降のtodo
- 期限なしで、作成から2日以上残っているtodo

期限が未来に設定されているtodoは、作成から2日以上経っても因数分解対象にはしません。

---

# 第5章 設定を変える

設定はすべて `#設定` チャンネルに普通に書くだけです。

## 5-1. 時刻を変える

```text
朝: 07:30
夜: 22:45
事前通知: 15
```

成功すると、こんな返信が返ります。

```text
先生、設定変数を更新しました。計算通りです。
- 朝の作戦会議: 07:30
- 夜の締め: 22:45
- 事前通知: 15分前
```

⚠️ ターミナル用のコマンドは `#設定` では実行できません。`npm run register` などはPCのPowerShell（ターミナル）で実行するものです。

## 5-2. 事前通知を設定する

**毎回15分前に事前通知したい場合：**

```text
事前通知: 15
```

**事前通知を使わない場合：**

```text
事前通知: なし
```

**登録のたびに決めたい場合：**

```text
事前通知モード: 毎回
```

このモードでは、todoやリマインダーの登録確認に `事前通知を設定` と `事前通知を設定しない` ボタンが出ます。

- `事前通知を設定しない` — その登録だけ事前通知なしになります
- `事前通知を設定` — 入力欄が開き、次のように指定できます

```text
なし
15分前
15ふんまえ
1時間前
120
```

数字だけ入力した場合は「分」として扱います。たとえば `120` は120分前、つまり2時間前です。

**固定したい場合：**

```text
事前通知モード: 固定
```

固定モードでは `#設定` の分数を自動適用します。

**完全にオフにしたい場合：**

```text
事前通知モード: オフ
```

指定時刻が来た後の再通知は、どのモードでも通知メッセージのボタンから選べます。

## 5-3. キャラクター設定を入れ替える

`#設定` に `SOUL.md` のようなキャラクター設定ファイルを添付します。

想定ファイル名の例：

```text
hayase_yuuka_SOUL.md
```

成功すると、botがこう返します。

```text
先生、人格設定を更新しました。
ファイル: hayase_yuuka_SOUL.md
これで私の応答方針は再計算済みです。
```

新しいMDファイルを添付すると上書きされます。

---

# 第6章 困った時

## 6-1. 確認ボタンが期限切れになった

todo・リマインダー・支出候補などの確認ボタンは**30分で期限切れ**になります。

期限切れ後にボタンを押すと、登録はできませんが、内容は表示されます。もう一度登録し直す時は、その内容を見ながら書き直せます。

## 6-2. botが反応しない時のチェックリスト

上から順に確認してください。

1. `#つぶやき` に書いていないか（そこはbotが反応しないチャンネルです）
2. botがそのチャンネルを**見られる権限**を持っているか
3. botがメッセージを**送れる権限**を持っているか
4. Discord Developer Portalで `MESSAGE CONTENT INTENT` がONになっているか
5. **1分待ったか**（自然文への反応は最大1分ほど遅れます）
6. `#設定` にキャラ設定を添付した場合、ファイル名が `.md` で終わっているか
7. `#設定` にターミナル用コマンドを貼っていないか（それはPowerShellで実行するものです）

なお、ユウカが「**今は少し頭がうまく回りません**」と返した場合は、AI（Gemini）側の一時的な不調です。故障ではないので、少し時間をおいてもう一度話しかけてください。不調が30分以上続く場合は、`#報告` にユウカからお知らせが届きます。

## 6-3. まだできないこと

以下は設計案にはありますが、現状ではまだできません。

- 外部カレンダー連携
